import React from 'react'
import { connect } from 'react-redux'
import { archiveEmail } from '../actions'

const ArchiveEmail = ({ dispatch }) => {
    return (
        <div>
            <button 
            onClick={e.preventDefault()
                if(){

                }
                dispatch(archiveEmail())

            }></button>
        </div>
    )
}